// 주석 Commetns
// 한줄 짜리 주석을 작성할 때 씀
// TODO(모니카):해야할 일을 작성
// TODO(모니카):XX기능 구현하기

/**
 * 주석은 코드 자체를 설명하는 것이 아니라,
 * 왜(WHY) 어떻게(HOW)를 설명하는 것이 좋음
 * 단, 정말 필요한 경우에만
 */

// 외부에서 많이 쓰이는 함수 API의 경우, jsdoc.js쓰는 것이 좋음

/**
 * 주어진 두 인자를 더한 값을 반환함
 * @param {*} a 숫자1
 * @param {*} b 숫자2
 * @returns a와 b를 더한값
 */
function add(a, b) {
  return a + b;
}

[].map;
